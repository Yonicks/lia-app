# Talki Nightly Report — 2026-08-21

Baseline:
- Tests: `tests/test_suite.py` — **22/22 checks pass** (layout, screens, gameplay, storage, PWA).
- JS errors: none on home / category / games at 390×844.
- Broken assets: none (all icon + splash links resolve, 16 files precached).
- Gameplay blockers: none found by the existing suite — but the suite drives most
  games with programmatic `.click()`, so real-pointer problems are invisible to it.
- Mobile layout issues: none reported at 390/834/1440/844-landscape by the
  existing overflow check (which only inspects `main *`).
- Accessibility issues: to be audited.
- Performance concerns: **event-listener leak on the persistent bottom nav** (see B1).
- Privacy/store concerns: to be audited.

Environment note: this sandbox has a prebuilt Chromium at
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`; the suites now accept a
`CHROMIUM_PATH` env var so they can run without `playwright install`.

---

## Bugs found

(filled in per iteration below)

