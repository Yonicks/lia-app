export type { RecognitionResult, SpeechRecognitionService } from './SpeechRecognitionService';

// Deliberately NO export of `poc/heIlRecognitionPoc.ts` here — it must stay
// unreferenced by application code (phase-04-plan.md, "The POC must not be
// imported by any application code").
