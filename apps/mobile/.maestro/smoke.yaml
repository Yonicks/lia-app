appId: com.yonicks.talki
---
# Phase 1 originally asserted the placeholder text "Talki Native
# Migration"; Phase 7 replaced it with real Home (app/index.tsx),
# so this smoke flow now asserts the real landing screen instead.
- launchApp
- assertVisible:
    id: 'home-root'
- takeScreenshot: phase-01-bootstrap
